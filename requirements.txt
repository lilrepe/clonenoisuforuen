torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
pywavelets>=1.4.0       # Wavelet features: pywt.wavedec (§Feature extraction)
scikit-learn>=1.3.0     # PCA, StandardScaler, metrics
imbalanced-learn>=0.11.0  # SMOTE (§Data preparation, Eq. 14)
pyyaml>=6.0             # Config loading

# Optional — for data loading
# mne>=1.6.0            # Recommended for OpenNeuro BIDS EEG loading
# scipy                 # For loading .mat files (OSF dataset)

# Optional — for PSO feature selection (src/fusion.py stub)
# pyswarms>=1.3.0       # PSO library; hyperparameters must be tuned manually
